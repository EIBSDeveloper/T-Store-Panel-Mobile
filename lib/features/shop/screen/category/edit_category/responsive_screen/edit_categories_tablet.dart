import '../../../../../../utils/constants/path_provider.dart';

class EditCategoriesTabletScreen extends StatelessWidget {
  const EditCategoriesTabletScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("Tablet"),
      ),
    );
  }
}
