import '../../../../../../utils/constants/path_provider.dart';

class EditCategoriesMobileScreen extends StatelessWidget {
  const EditCategoriesMobileScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("Mobile"),
      ),
    );
  }
}
