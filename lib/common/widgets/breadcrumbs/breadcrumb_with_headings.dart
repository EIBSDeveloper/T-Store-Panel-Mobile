import '../../../utils/constants/path_provider.dart';

class BreadcrumbWithHeadings extends StatelessWidget {
  const BreadcrumbWithHeadings({super.key});

  @override
  Widget build(BuildContext context) {
    return Column(
      children: [],
    );
  }
}
